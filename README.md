# Cruds
مشروع CRUDs هو تطبيق لإدارة البيانات بشكل كامل، يتيح للمستخدم إضافة (Create)، عرض (Read)، تعديل (Update)، وحذف (Delete) السجلات بطريقة سهلة وواجهة تفاعلية، مع تخزين البيانات محليًا أو على قاعدة بيانات
